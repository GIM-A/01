1. 注册 DeepSeek 账号，拿到 API Key，本地存到`.env`，**严禁上传密钥到 GitHub**
2. 搭建 Python 虚拟环境，安装依赖

```
pip install langchain langchain-deepseek chromadb python-dotenv gradio
```

3. GitHub 新建仓库 `simple-react-agent`，建好 README、.gitignore 模板
4. 复习基础：JSON、API 调用、Python 函数（如果遗忘，简单过一遍）
